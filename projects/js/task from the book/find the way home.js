let landmarks = [];
landmarks.push("Мой дом");
landmarks.push("Дорожка к дому");
landmarks.push("Мигающий фонарь");
landmarks.push("Протекающий гидрант");
landmarks.push("Пожарная станция");
landmarks.push("Приют для кошек");
landmarks.push("Моя бывшая школа");
landmarks.push("Дом подруги");

console.log(landmarks);

landmarks.pop();
landmarks.pop();
landmarks.pop();
landmarks.pop();
landmarks.indexOf("Протекающий гидрант");
landmarks[3];
landmarks.length


let randomIndex = Math.floor(Math.random() * landmarks.length);
 landmarks[randomIndex];

 // landmarks[Math.floor(Math.random() * landmarks.length)];
// короткая запись

