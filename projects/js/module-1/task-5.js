const padding =  Number.parseInt('20px');
const border = Number.parseInt('5px');
const contentWidth = Number.parseInt('100px');

const totalWidth = `${padding + border + contentWidth}px` ;

console.log(totalWidth);
